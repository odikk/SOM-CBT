function select_all_none(e, cat) {  var fform = document.dirform;  if (fform == null){     return;  }  var ffl = fform.elements.length;  var suffix = 'q_' + cat + '_';  var len = suffix.length;  for (i = 0; i < ffl; i++){    if (fform.elements[i].name.substring(0, len) == suffix &&        fform.elements[i].disabled == false) {      fform.elements[i].checked = e.checked;    }  }}function pick_que(){  var listObj = this;  if (listObj == null || listObj.document == null){     return;  }  var fform = document.dirform;  if (fform == null){     return -1;  }  var ffl = fform.elements.length;  var question_array = new Array();  var question_index = 0;  var ffe;  for (i = 0; i < ffl; i++){    ffe = fform.elements[i];    if (ffe.checked)    {      if (ffe.name.substring(0, 2) == 'q_'          || ffe.name.substring(0, 4) == 'cat_')      {        question_array[question_index++] = ffe.name;      }    }  }  if (question_index == 0) {     alert("Please select at least one question.")     return  }  fform.question_order.value = question_array.join(',');  fform.submit();  return 1;}