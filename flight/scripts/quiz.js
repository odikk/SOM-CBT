function click_quiz() {	if (window.document.settings.quiz.checked) {		window.document.settings.seq.checked = false;		window.document.settings.rand.checked = true;//these were reversed by SMQ so that "randomize" would be selected by default - SMQ	}	else if (window.document.settings.quiz.checked == false ) {		window.document.settings.seq.checked = false;		window.document.settings.rand.checked = false;			}}function click_seq() {	if (window.document.settings.seq.checked) {		window.document.settings.quiz.checked = true;		window.document.settings.rand.checked = false;	}	enable_disable_quiz();	}function click_random() {	if (window.document.settings.rand.checked) {		window.document.settings.quiz.checked = true;		window.document.settings.seq.checked = false;	}	enable_disable_quiz();}function enable_disable_quiz(){	if ( (window.document.settings.rand.checked == false) && (window.document.settings.seq.checked == false) ){		window.document.settings.quiz.checked = false;	}}function click_review() {	if (window.document.settings.review.checked) {		window.document.settings.incorrect.checked = true;		window.document.settings.r_all.checked = false;	}	else if (window.document.settings.review.checked == false ) {		window.document.settings.incorrect.checked = false;		window.document.settings.r_all.checked = false;			}}function click_inc() {	if (window.document.settings.incorrect.checked) {		window.document.settings.review.checked = true;		window.document.settings.r_all.checked = false;	}	enable_disable_preview();	}function click_all() {	if (window.document.settings.r_all.checked) {		window.document.settings.review.checked = true;		window.document.settings.incorrect.checked = false;	}	enable_disable_preview();}function enable_disable_preview(){	if ( (window.document.settings.r_all.checked == false) && (window.document.settings.incorrect.checked == false) ){		window.document.settings.review.checked = false;	}}function allow_now(which) {	var form = window.document.settings ;	var result = form.time_now.value ;	result = result.split(",");	select_array_index_by_value(form[(which+"date")], result[0]);	select_array_index_by_value(form[(which+"month")], result[1]);		select_array_index_by_value(form[(which+"year")], result[2]);		select_array_index_by_value(form[(which+"hour")], result[4]);		select_array_index_by_value(form[(which+"min")], result[5]);}function select_array_index_by_value(arr, val){	for (i = 0; i < arr.length; i++)    	if (arr[i].value == val)      		arr[i].selected = true;}function check_quiz_review() {	var form = window.document.settings ;	if ((form.review.checked) && (form.quiz.checked)) {		alert("Both Quiz and review cannot be available at the same time");		return false;	}	submit_form(document.settings, 'save');	return true;}function do_confirm(op, path_url) {	if (op == 'reset') {		input = confirm("Confirm Reset");		if (input==true) {			window.location = path_url;			return true;		} else {			return false;		}	}	window.location = path_url;	return true;}function instance_check() {	var old = form.oldname.value;	var inbox = form.qname.value;	if(old == inbox) {		alert("To create a new test instance, the quiz name must be different than the original name");		return false;	}	submit_form(document.settings, 'instance');	return true;}