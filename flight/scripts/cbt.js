function select_some(form_name, field_name, val){  var ffl = form_name.elements.length;  var question_array = new Array();  var question_index = 0;  var ffe;  for (i = 0; i < ffl; i++){    ffe = form_name.elements[i];    if (ffe.checked)    {        question_array[question_index++] = ffe.value;    }  }  if (question_index == 0) {     alert("Please select at least one .")     return  }  form_name[field_name].value = question_array.join(',');  return submit_form(form_name, val);}function select_one(form_name, field_name, val){  var ffl = form_name.elements.length;  var question_array = new Array();  var question_index = 0;  var ffe;  for (i = 0; i < ffl; i++){    ffe = form_name.elements[i];    if (ffe.type == 'checkbox') {	    if (ffe.checked)    	{			question_array[question_index] = ffe.value;			question_index++;	    }	}  }  if (question_index == 1) {	  form_name[field_name].value = question_array.join(',');	  return submit_form(form_name, val)  }else if( (question_index == 0) || (question_index > 1) ) {  	alert("Please select One .");  }	return false;}function submit_form(form_name, val) {	form_name.action.value = val ;	if (val == 'del') {		input = confirm("Confirm Deletion");		if (input==true) {			form_name.submit();			return true;		}else {			return false;		}	}	form_name.submit();	return true;}function popUp(URL,width,height,resize, statusbar) {day = new Date();id = day.getTime();eval("page" + id + " = window.open(URL, '" + id + "','toolbar=0,scrollbars=1,location=0,left=0,top=0, statusbar="+statusbar+",menubar=0,resizable="+resize+",width="+width+",height="+height+"');");}function popUp2(URL) {day = new Date();id = day.getTime();eval("page" + id + " = window.open(URL, '" + id + "','toolbar=0,scrollbars=1,location=0,left=0, top=0, statusbar=1,menubar=0,resizable=1');");}function whoselected(elem) {sel_elem = elem;}